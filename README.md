This demo uses hosted placeholder images (picsum.photos) referenced directly
in index.html and script.js, so no local image files are required to run
the site. Drop your own product photos in this folder and update the
image paths in script.js (the `seed` field / imgUrl function) once you
have real seller-submitted images to use instead of placeholders.
